// Code generated by SQLBoiler 4.19.7 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// CrawlSyncStatusCount is an object representing the database table.
type CrawlSyncStatusCount struct {
	// A unique id that identifies this record
	ID int `boil:"id" json:"id" toml:"id" yaml:"id"`
	// Reference to the crawl this count is for
	CrawlID int `boil:"crawl_id" json:"crawl_id" toml:"crawl_id" yaml:"crawl_id"`
	// Number of peers that are synced
	SyncedCount int `boil:"synced_count" json:"synced_count" toml:"synced_count" yaml:"synced_count"`
	// Number of peers that are not synced
	NotSyncedCount int `boil:"not_synced_count" json:"not_synced_count" toml:"not_synced_count" yaml:"not_synced_count"`
	// Number of peers with unknown sync status
	UnknownCount int `boil:"unknown_count" json:"unknown_count" toml:"unknown_count" yaml:"unknown_count"`
	// Timestamp when this record was created
	CreatedAt time.Time `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`
	// Total number of unique peers in the crawl
	TotalCount int `boil:"total_count" json:"total_count" toml:"total_count" yaml:"total_count"`

	R *crawlSyncStatusCountR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L crawlSyncStatusCountL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var CrawlSyncStatusCountColumns = struct {
	ID             string
	CrawlID        string
	SyncedCount    string
	NotSyncedCount string
	UnknownCount   string
	CreatedAt      string
	TotalCount     string
}{
	ID:             "id",
	CrawlID:        "crawl_id",
	SyncedCount:    "synced_count",
	NotSyncedCount: "not_synced_count",
	UnknownCount:   "unknown_count",
	CreatedAt:      "created_at",
	TotalCount:     "total_count",
}

var CrawlSyncStatusCountTableColumns = struct {
	ID             string
	CrawlID        string
	SyncedCount    string
	NotSyncedCount string
	UnknownCount   string
	CreatedAt      string
	TotalCount     string
}{
	ID:             "crawl_sync_status_counts.id",
	CrawlID:        "crawl_sync_status_counts.crawl_id",
	SyncedCount:    "crawl_sync_status_counts.synced_count",
	NotSyncedCount: "crawl_sync_status_counts.not_synced_count",
	UnknownCount:   "crawl_sync_status_counts.unknown_count",
	CreatedAt:      "crawl_sync_status_counts.created_at",
	TotalCount:     "crawl_sync_status_counts.total_count",
}

// Generated where

var CrawlSyncStatusCountWhere = struct {
	ID             whereHelperint
	CrawlID        whereHelperint
	SyncedCount    whereHelperint
	NotSyncedCount whereHelperint
	UnknownCount   whereHelperint
	CreatedAt      whereHelpertime_Time
	TotalCount     whereHelperint
}{
	ID:             whereHelperint{field: "\"crawl_sync_status_counts\".\"id\""},
	CrawlID:        whereHelperint{field: "\"crawl_sync_status_counts\".\"crawl_id\""},
	SyncedCount:    whereHelperint{field: "\"crawl_sync_status_counts\".\"synced_count\""},
	NotSyncedCount: whereHelperint{field: "\"crawl_sync_status_counts\".\"not_synced_count\""},
	UnknownCount:   whereHelperint{field: "\"crawl_sync_status_counts\".\"unknown_count\""},
	CreatedAt:      whereHelpertime_Time{field: "\"crawl_sync_status_counts\".\"created_at\""},
	TotalCount:     whereHelperint{field: "\"crawl_sync_status_counts\".\"total_count\""},
}

// CrawlSyncStatusCountRels is where relationship names are stored.
var CrawlSyncStatusCountRels = struct {
	Crawl string
}{
	Crawl: "Crawl",
}

// crawlSyncStatusCountR is where relationships are stored.
type crawlSyncStatusCountR struct {
	Crawl *Crawl `boil:"Crawl" json:"Crawl" toml:"Crawl" yaml:"Crawl"`
}

// NewStruct creates a new relationship struct
func (*crawlSyncStatusCountR) NewStruct() *crawlSyncStatusCountR {
	return &crawlSyncStatusCountR{}
}

func (o *CrawlSyncStatusCount) GetCrawl() *Crawl {
	if o == nil {
		return nil
	}

	return o.R.GetCrawl()
}

func (r *crawlSyncStatusCountR) GetCrawl() *Crawl {
	if r == nil {
		return nil
	}

	return r.Crawl
}

// crawlSyncStatusCountL is where Load methods for each relationship are stored.
type crawlSyncStatusCountL struct{}

var (
	crawlSyncStatusCountAllColumns            = []string{"id", "crawl_id", "synced_count", "not_synced_count", "unknown_count", "created_at", "total_count"}
	crawlSyncStatusCountColumnsWithoutDefault = []string{"crawl_id"}
	crawlSyncStatusCountColumnsWithDefault    = []string{"id", "synced_count", "not_synced_count", "unknown_count", "created_at", "total_count"}
	crawlSyncStatusCountPrimaryKeyColumns     = []string{"id"}
	crawlSyncStatusCountGeneratedColumns      = []string{"id"}
)

type (
	// CrawlSyncStatusCountSlice is an alias for a slice of pointers to CrawlSyncStatusCount.
	// This should almost always be used instead of []CrawlSyncStatusCount.
	CrawlSyncStatusCountSlice []*CrawlSyncStatusCount

	crawlSyncStatusCountQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	crawlSyncStatusCountType                 = reflect.TypeOf(&CrawlSyncStatusCount{})
	crawlSyncStatusCountMapping              = queries.MakeStructMapping(crawlSyncStatusCountType)
	crawlSyncStatusCountPrimaryKeyMapping, _ = queries.BindMapping(crawlSyncStatusCountType, crawlSyncStatusCountMapping, crawlSyncStatusCountPrimaryKeyColumns)
	crawlSyncStatusCountInsertCacheMut       sync.RWMutex
	crawlSyncStatusCountInsertCache          = make(map[string]insertCache)
	crawlSyncStatusCountUpdateCacheMut       sync.RWMutex
	crawlSyncStatusCountUpdateCache          = make(map[string]updateCache)
	crawlSyncStatusCountUpsertCacheMut       sync.RWMutex
	crawlSyncStatusCountUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

// One returns a single crawlSyncStatusCount record from the query.
func (q crawlSyncStatusCountQuery) One(ctx context.Context, exec boil.ContextExecutor) (*CrawlSyncStatusCount, error) {
	o := &CrawlSyncStatusCount{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for crawl_sync_status_counts")
	}

	return o, nil
}

// All returns all CrawlSyncStatusCount records from the query.
func (q crawlSyncStatusCountQuery) All(ctx context.Context, exec boil.ContextExecutor) (CrawlSyncStatusCountSlice, error) {
	var o []*CrawlSyncStatusCount

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to CrawlSyncStatusCount slice")
	}

	return o, nil
}

// Count returns the count of all CrawlSyncStatusCount records in the query.
func (q crawlSyncStatusCountQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count crawl_sync_status_counts rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q crawlSyncStatusCountQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if crawl_sync_status_counts exists")
	}

	return count > 0, nil
}

// Crawl pointed to by the foreign key.
func (o *CrawlSyncStatusCount) Crawl(mods ...qm.QueryMod) crawlQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.CrawlID),
	}

	queryMods = append(queryMods, mods...)

	return Crawls(queryMods...)
}

// LoadCrawl allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (crawlSyncStatusCountL) LoadCrawl(ctx context.Context, e boil.ContextExecutor, singular bool, maybeCrawlSyncStatusCount any, mods queries.Applicator) error {
	var slice []*CrawlSyncStatusCount
	var object *CrawlSyncStatusCount

	if singular {
		var ok bool
		object, ok = maybeCrawlSyncStatusCount.(*CrawlSyncStatusCount)
		if !ok {
			object = new(CrawlSyncStatusCount)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeCrawlSyncStatusCount)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeCrawlSyncStatusCount))
			}
		}
	} else {
		s, ok := maybeCrawlSyncStatusCount.(*[]*CrawlSyncStatusCount)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeCrawlSyncStatusCount)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeCrawlSyncStatusCount))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &crawlSyncStatusCountR{}
		}
		args[object.CrawlID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &crawlSyncStatusCountR{}
			}

			args[obj.CrawlID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`crawls`),
		qm.WhereIn(`crawls.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Crawl")
	}

	var resultSlice []*Crawl
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Crawl")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for crawls")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for crawls")
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Crawl = foreign
		if foreign.R == nil {
			foreign.R = &crawlR{}
		}
		foreign.R.CrawlSyncStatusCount = object
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.CrawlID == foreign.ID {
				local.R.Crawl = foreign
				if foreign.R == nil {
					foreign.R = &crawlR{}
				}
				foreign.R.CrawlSyncStatusCount = local
				break
			}
		}
	}

	return nil
}

// SetCrawl of the crawlSyncStatusCount to the related item.
// Sets o.R.Crawl to related.
// Adds o to related.R.CrawlSyncStatusCount.
func (o *CrawlSyncStatusCount) SetCrawl(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Crawl) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"crawl_sync_status_counts\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"crawl_id"}),
		strmangle.WhereClause("\"", "\"", 2, crawlSyncStatusCountPrimaryKeyColumns),
	)
	values := []any{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.CrawlID = related.ID
	if o.R == nil {
		o.R = &crawlSyncStatusCountR{
			Crawl: related,
		}
	} else {
		o.R.Crawl = related
	}

	if related.R == nil {
		related.R = &crawlR{
			CrawlSyncStatusCount: o,
		}
	} else {
		related.R.CrawlSyncStatusCount = o
	}

	return nil
}

// CrawlSyncStatusCounts retrieves all the records using an executor.
func CrawlSyncStatusCounts(mods ...qm.QueryMod) crawlSyncStatusCountQuery {
	mods = append(mods, qm.From("\"crawl_sync_status_counts\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"crawl_sync_status_counts\".*"})
	}

	return crawlSyncStatusCountQuery{q}
}

// FindCrawlSyncStatusCount retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindCrawlSyncStatusCount(ctx context.Context, exec boil.ContextExecutor, iD int, selectCols ...string) (*CrawlSyncStatusCount, error) {
	crawlSyncStatusCountObj := &CrawlSyncStatusCount{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"crawl_sync_status_counts\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, crawlSyncStatusCountObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from crawl_sync_status_counts")
	}

	return crawlSyncStatusCountObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *CrawlSyncStatusCount) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no crawl_sync_status_counts provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
	}

	nzDefaults := queries.NonZeroDefaultSet(crawlSyncStatusCountColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	crawlSyncStatusCountInsertCacheMut.RLock()
	cache, cached := crawlSyncStatusCountInsertCache[key]
	crawlSyncStatusCountInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			crawlSyncStatusCountAllColumns,
			crawlSyncStatusCountColumnsWithDefault,
			crawlSyncStatusCountColumnsWithoutDefault,
			nzDefaults,
		)
		wl = strmangle.SetComplement(wl, crawlSyncStatusCountGeneratedColumns)

		cache.valueMapping, err = queries.BindMapping(crawlSyncStatusCountType, crawlSyncStatusCountMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(crawlSyncStatusCountType, crawlSyncStatusCountMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"crawl_sync_status_counts\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"crawl_sync_status_counts\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into crawl_sync_status_counts")
	}

	if !cached {
		crawlSyncStatusCountInsertCacheMut.Lock()
		crawlSyncStatusCountInsertCache[key] = cache
		crawlSyncStatusCountInsertCacheMut.Unlock()
	}

	return nil
}

// Update uses an executor to update the CrawlSyncStatusCount.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *CrawlSyncStatusCount) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	key := makeCacheKey(columns, nil)
	crawlSyncStatusCountUpdateCacheMut.RLock()
	cache, cached := crawlSyncStatusCountUpdateCache[key]
	crawlSyncStatusCountUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			crawlSyncStatusCountAllColumns,
			crawlSyncStatusCountPrimaryKeyColumns,
		)
		wl = strmangle.SetComplement(wl, crawlSyncStatusCountGeneratedColumns)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update crawl_sync_status_counts, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"crawl_sync_status_counts\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, crawlSyncStatusCountPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(crawlSyncStatusCountType, crawlSyncStatusCountMapping, append(wl, crawlSyncStatusCountPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update crawl_sync_status_counts row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for crawl_sync_status_counts")
	}

	if !cached {
		crawlSyncStatusCountUpdateCacheMut.Lock()
		crawlSyncStatusCountUpdateCache[key] = cache
		crawlSyncStatusCountUpdateCacheMut.Unlock()
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values.
func (q crawlSyncStatusCountQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for crawl_sync_status_counts")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for crawl_sync_status_counts")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o CrawlSyncStatusCountSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]any, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), crawlSyncStatusCountPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"crawl_sync_status_counts\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, crawlSyncStatusCountPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in crawlSyncStatusCount slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all crawlSyncStatusCount")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *CrawlSyncStatusCount) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("models: no crawl_sync_status_counts provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
	}

	nzDefaults := queries.NonZeroDefaultSet(crawlSyncStatusCountColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	crawlSyncStatusCountUpsertCacheMut.RLock()
	cache, cached := crawlSyncStatusCountUpsertCache[key]
	crawlSyncStatusCountUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			crawlSyncStatusCountAllColumns,
			crawlSyncStatusCountColumnsWithDefault,
			crawlSyncStatusCountColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			crawlSyncStatusCountAllColumns,
			crawlSyncStatusCountPrimaryKeyColumns,
		)

		insert = strmangle.SetComplement(insert, crawlSyncStatusCountGeneratedColumns)
		update = strmangle.SetComplement(update, crawlSyncStatusCountGeneratedColumns)

		if updateOnConflict && len(update) == 0 {
			return errors.New("models: unable to upsert crawl_sync_status_counts, could not build update column list")
		}

		ret := strmangle.SetComplement(crawlSyncStatusCountAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(crawlSyncStatusCountPrimaryKeyColumns) == 0 {
				return errors.New("models: unable to upsert crawl_sync_status_counts, could not build conflict column list")
			}

			conflict = make([]string, len(crawlSyncStatusCountPrimaryKeyColumns))
			copy(conflict, crawlSyncStatusCountPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"crawl_sync_status_counts\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(crawlSyncStatusCountType, crawlSyncStatusCountMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(crawlSyncStatusCountType, crawlSyncStatusCountMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []any
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "models: unable to upsert crawl_sync_status_counts")
	}

	if !cached {
		crawlSyncStatusCountUpsertCacheMut.Lock()
		crawlSyncStatusCountUpsertCache[key] = cache
		crawlSyncStatusCountUpsertCacheMut.Unlock()
	}

	return nil
}

// Delete deletes a single CrawlSyncStatusCount record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *CrawlSyncStatusCount) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no CrawlSyncStatusCount provided for delete")
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), crawlSyncStatusCountPrimaryKeyMapping)
	sql := "DELETE FROM \"crawl_sync_status_counts\" WHERE \"id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from crawl_sync_status_counts")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for crawl_sync_status_counts")
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q crawlSyncStatusCountQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no crawlSyncStatusCountQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from crawl_sync_status_counts")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for crawl_sync_status_counts")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o CrawlSyncStatusCountSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	var args []any
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), crawlSyncStatusCountPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"crawl_sync_status_counts\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, crawlSyncStatusCountPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from crawlSyncStatusCount slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for crawl_sync_status_counts")
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *CrawlSyncStatusCount) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindCrawlSyncStatusCount(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *CrawlSyncStatusCountSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := CrawlSyncStatusCountSlice{}
	var args []any
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), crawlSyncStatusCountPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"crawl_sync_status_counts\".* FROM \"crawl_sync_status_counts\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, crawlSyncStatusCountPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in CrawlSyncStatusCountSlice")
	}

	*o = slice

	return nil
}

// CrawlSyncStatusCountExists checks if the CrawlSyncStatusCount row exists.
func CrawlSyncStatusCountExists(ctx context.Context, exec boil.ContextExecutor, iD int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"crawl_sync_status_counts\" where \"id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if crawl_sync_status_counts exists")
	}

	return exists, nil
}

// Exists checks if the CrawlSyncStatusCount row exists.
func (o *CrawlSyncStatusCount) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return CrawlSyncStatusCountExists(ctx, exec, o.ID)
}
